{
  "name": "iterate-minor-version",
  "version": "1.0.0",
  "description": "Returns a list of all patch versions up to the supplied version within this minor version",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ModdingForBlockheads/iterate-minor-version.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ModdingForBlockheads/iterate-minor-version/issues"
  },
  "homepage": "https://github.com/ModdingForBlockheads/iterate-minor-version#readme",
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "eslint": "^8.25.0",
    "jest": "^29.2.0"
  }
}
