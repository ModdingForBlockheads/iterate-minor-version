{
  "name": "list-compatible-mc-versions",
  "version": "1.0.0",
  "description": "A list of all patch versions compatible with the supplied minor version (e.g. 1.19, 1.19.1, 1.19.2)",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ModdingForBlockheads/list-compatible-mc-versions.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ModdingForBlockheads/list-compatible-mc-versions/issues"
  },
  "homepage": "https://github.com/ModdingForBlockheads/list-compatible-mc-versions#readme",
  "dependencies": {
    "@actions/core": "^1.11.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.2",
    "eslint": "^9.11.1",
    "jest": "^29.7.0"
  }
}
